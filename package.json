{
  "name": "sayit",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev pages --ip 127.0.0.1 --port 4173 --live-reload",
    "dev:static": "python3 -m http.server 4173 --bind 127.0.0.1 -d pages",
    "test": "node --test test/*.test.js",
    "test:smoke": "node test/preview-smoke.mjs"
  }
}
